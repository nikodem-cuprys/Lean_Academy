/**
 * Placeholder package — not yet implemented.
 *
 * Will hold accepted psychometric calculations (d-prime, hit/false-alarm
 * rate, span scoring, switch cost, confidence intervals for Phase 6
 * assessments) — see "PERFORMANCE METRICS" in project_prompt.txt:
 * "Do not invent pseudoscientific metrics when accepted measures exist."
 *
 * Every function here should cite the accepted formula it implements in
 * a comment, and get dedicated unit tests (docs/testing.md calls out
 * scientific-calculation tests as their own test category).
 */
export const PSYCHOMETRICS_PLACEHOLDER = true;
