# Kanban — LeanAcademy

_Companion to `docs/development-plan.md` and `docs/scrum.md`. Columns: Backlog → Ready → Done. WIP limit: 1 card in Ready at a time. "Review/Testing" isn't a separate column — it's folded into the Definition of Done every card must satisfy before moving to Done (see below). This file is the live source of truth for "what's next" — keep it current rather than letting `docs/development-plan.md` (phase-level) or `docs/scrum.md` (process-level) go stale as a substitute._

## Epics

`Research` · `Design System` · `Onboarding` · `Authentication` · `Working Memory` · `Reading Training` · `Adaptive Engine` · `Daily Training` · `Progress` · `Gamification` · `Science/Evidence` · `Payments` · `Mobile Foundation` · `iOS` · `Android` · `Analytics` · `Infrastructure` · `Testing` · `UX Polish`

## Board

## Definition of Done

Every card moves to Done only after all of the following are true (this is what "Review/Testing" collapses into — there is no separate column for it):

1. **Real browser verification** — Claude-in-Chrome first; Playwright fallback in `apps/web/e2e/` against a production build if it won't connect (per CLAUDE.md's Commands section). State plainly what was and wasn't actually exercised.
2. **Full existing e2e suite still green**, run alongside the new/changed spec, not just in isolation.
3. **Evidence-gating chain updated together, in the same change**, if the card adds/changes a training method (`docs/evidence-review.md` + `data/evidence-registry.json` + re-seed) — see CLAUDE.md's Architecture section.
4. **Non-negotiable content rules spot-checked** (no IQ/intelligence/dementia/ADHD claims, trained-task/near/far-transfer kept separate, WPM never without comprehension, difficulty always shown in plain language) — see CLAUDE.md.
5. **This file updated with the Done entry**, in the same narrative style as the existing Done log.

Cross-referenced from `docs/scrum.md`'s Definition of Done section, which does not repeat this list.

### Done

_Entries below are narrative, not the Card template — that's intentional and has worked well; keep new entries in the same style rather than retrofitting the template onto prose. See Definition of Done above for what every entry here has already satisfied._

- **[Research] Literature review pass 1** — WM updating, complex span, visuospatial WM, verbal WM, cognitive control, reading efficiency, memory strategies. Owner: Cognitive Scientist. Output: `docs/evidence-review.md`, `data/evidence-registry.json`.
- **[Research] Evidence registry v0** — machine-readable registry gating the exercise catalog. Owner: Data Engineer / Psychometrics Specialist. Output: `data/evidence-registry.json`.
- **[Research] Product Requirements Document v0** — Owner: Product Manager. Output: `docs/product-requirements.md`.
- **[Design System] UX strategy + journey map v0** — 14 core screens, IA, critical path. Owner: UX Researcher / Senior UI/UX Designer. Output: `docs/ux-strategy.md`.
- **[Design System] Design system v0 (tokens/typography/components/motion/a11y)** — Owner: Senior UI/UX Designer / Product Designer. Output: `docs/design-system.md`.
- **[Infrastructure] Development plan + phased roadmap** — Owner: Product Manager. Output: `docs/development-plan.md`.
- **[Infrastructure] Kanban board** — this file.
- **[Mobile Foundation] Mobile plan v0** — Owner: Mobile Engineer. Output: `docs/mobile-plan.md`.
- **[Payments] Monetization plan v0** — Owner: Monetization Strategist. Output: `docs/monetization-plan.md`.
- **[Infrastructure] Security plan v0** — Owner: Security Engineer. Output: `docs/security.md`.
- **[Testing] Test strategy v0** — Owner: QA Engineer. Output: `docs/testing.md`.
- **[Infrastructure] Repository & Monorepo Scaffold** — pnpm workspaces (`apps/{web,api}` + 8 `packages/*`). apps/web (Next.js 16, App Router) boots and renders real evidence-registry + auth-session data; apps/api (Fastify) serves `/health` and `/catalog`. `pnpm build`/`typecheck`/`test`/`lint` all pass at the repo root. Owner: Senior Full-Stack Engineer. See CLAUDE.md's Commands section for the real commands this unblocked.
- **[Infrastructure] Database Schema v0** — Prisma schema in `packages/db/prisma/schema.prisma` covering every entity in `project_prompt.txt`'s DATABASE section. Two justified deviations: (1) `User`/`Account`/`Session`/`VerificationToken` follow Auth.js's standard adapter shape rather than a bespoke `Identity` table — `Account` already *is* the per-provider "Identity/AuthProvider" model the spec asked for, and reusing Auth.js's schema means the OAuth/PKCE code path is library-tested, not hand-rolled; (2) added `ChallengeProgress` as the join table normalization requires (not in the spec's literal entity list). Owner: Data Engineer.
- **[Science/Evidence] Evidence Registry Loader & Catalog Gate** — `packages/evidence` (Zod-validated, throws loudly on malformed/missing data). Split into `parseEvidenceRegistry` (pure, bundler-safe) and `loadEvidenceRegistry` (fs-based, Node-only) after discovering Next.js's server bundle virtualizes `__dirname`, which broke naive fs reads from bundled code — apps/web imports the JSON directly and parses it; apps/api reads it from disk. 9 tests, including one proving a non-approved module id is rejected. Owner: Data Engineer.
- **[Adaptive Engine] Adaptive Engine Skeleton** — `packages/adaptive-engine`'s `RollingWindowAdaptiveEngine` implements the exact `AdaptiveTrainingTask` interface from `project_prompt.txt`. 10 tests, including ones proving no single-trial difficulty swings and correct min/max clamping. Not yet wired to a real exercise (that's `packages/cognitive-engine`'s job, still a placeholder). Owner: Senior Full-Stack Engineer / Psychometrics Specialist.
- **[Infrastructure] Run the first real Prisma migration** — a local PostgreSQL 17 server (already installed, Windows service) now backs local dev: a dedicated `lean_academy` login role + database were created (not the `postgres` superuser), `pnpm db:migrate` applied migration `20260913190003_init` cleanly, and `pnpm --filter @lean-academy/db seed` synced all 10 evidence records. `apps/api`'s `/health` now does a real `SELECT 1` through Prisma and reports `{"status":"ok","db":"connected"}`. Credentials live in `.env` (repo root) and `packages/db/.env` (Prisma CLI doesn't read the root one — see CLAUDE.md) — both gitignored, neither committed. Owner: Data Engineer.
- **[Authentication] Auth: login/signup UI** — `/login` and `/signup` in `apps/web`, built as a shared `<AuthForm>` client component matching `prototype/Login.dc.html` (tab toggle, OAuth buttons, divider, fields, all styled from real design tokens now wired into `apps/web`'s Tailwind theme via `globals.css`). Verified against the real DB, not just rendered: registered a test user through `POST /api/auth/register`, confirmed a wrong password is rejected and the correct one succeeds and sets a session cookie (replicated the CSRF+cookie dance `next-auth/react`'s `signIn()` does internally), confirmed duplicate-email registration returns 409, then deleted the test user. Google/Facebook buttons call `signIn()` correctly but the actual OAuth redirect is unverified — there are no real OAuth app credentials configured (`GOOGLE_CLIENT_ID`/`FACEBOOK_CLIENT_ID` are still blank in `.env`), so don't treat that path as proven yet. Also discovered and documented: Auth.js rejects requests with `UntrustedHost` under `next start`/production mode unless `AUTH_TRUST_HOST=true` is set (`next dev` trusts automatically) — added to `.env`/`.env.example`. Email verification and password reset remain separately tracked below (not part of this card). Owner: Senior Full-Stack Engineer / Senior UI/UX Designer.
- **[Authentication] Auth: email verification + password reset delivery** — real SMTP delivery via `nodemailer`, not a console.log stand-in: locally it sends to `maildev` (`pnpm dev:mail`, a genuine local SMTP server + web UI at `http://localhost:1080` — see `CLAUDE.md`), and the same code points at a real transactional provider in production via the same `SMTP_*` env vars. Reuses Auth.js's `VerificationToken` table for both flows (`apps/web/src/lib/tokens.ts`), with the identifier prefixed by purpose (`verify:`/`reset:` + email) so they can't collide; tokens are stored as a SHA-256 hash, never plaintext, and deleted on first use. New routes: `/verify-email` (server component, verifies on render), `/forgot-password` + `/reset-password` (forms), plus `POST /api/auth/forgot-password` and `POST /api/auth/reset-password`. `forgot-password` always returns the same generic message regardless of whether the account exists or has a password at all, to prevent email enumeration, and is rate-limited (3 requests / 15 min per email) via a new minimal in-memory limiter (`apps/web/src/lib/rate-limit.ts` — single-instance only, not the full rate-limiting requirement from `docs/security.md`, which still needs a shared store for login/register). Verified against the real DB and real SMTP end to end, not just rendered: registered a user, fetched the actual email from maildev's API, followed the real verification link (`emailVerified` got set, token deleted, reusing the same link correctly failed), requested a password reset for both a real and a nonexistent email (identical response, only one email actually sent), used the real reset link to change the password (old password then rejected, new one accepted), confirmed reusing the reset link fails, and confirmed the rate limiter caps repeated requests. One known, documented gap: sessions use the JWT strategy, so a password reset does not invalidate sessions already issued on other devices — closing that would need database sessions or a revocation list. Owner: Senior Full-Stack Engineer / Security Engineer.
- **[Working Memory] Adaptive N-Back exercise logic** — `packages/cognitive-engine`'s `NBackTask` (visual/spatial 3x3-grid n-back, wrapping `RollingWindowAdaptiveEngine` — difficulty *is* N here, it owns no adaptation logic of its own). 8 tests: warm-up trials (first N stimuli) correctly excluded from scoring, all four signal-detection classifications (hit/miss/falseAlarm/correctRejection) traced through by hand with a deterministic injected `random`, difficulty confirmed unchanged after a single trial and stepping down by exactly one after a full window of misses (proving it defers to the existing engine rather than reimplementing adjustment), a statistical check that the observed match rate lands within tolerance of the theoretical rate (forced + incidental matches), and a grid-bounds check over 500 trials. Owner: Senior Full-Stack Engineer / Psychometrics Specialist.
- **[Working Memory] N-Back exercise screen (apps/web)** — `/train/n-back`, auth-gated, `<NBackExercise>` running a real 20-trial session against `NBackTask` with `performance.now()`-based stimulus/response timing, a `visibilitychange` listener that excludes an interrupted trial from scoring entirely (not just flags it), and a results screen with honest per-outcome copy (level-up/level-down/held-steady framed as adaptive-engine behavior, never as an ability claim). Two deliberate departures from `prototype/Exercise.dc.html`, both explained in the component's own header comment: the "Exercise 1 of 3" dots became a real "Trial N of 20" progress bar (no multi-exercise session exists yet to make dots honest), and the domain tag was corrected from the prototype's "SPATIAL SEQUENCE" label to "N-BACK" (matching what the task actually is). **Now verified in a real Chromium browser**, not just server-rendered HTML: added `apps/web/e2e/n-back.spec.ts` (Playwright — the E2E tool `docs/testing.md`/`project_prompt.txt` already call for; the Claude-in-Chrome extension still wasn't connecting, so this was the alternative). The test signs up a fresh user, plays a full 20-trial run by actually clicking the response button each trial (Playwright's auto-waiting click naturally paces itself to the button's real disabled/enabled cycle), and asserts the results screen. It caught a real, if narrow, finding along the way — see the dev-mode CSRF fix below — and confirmed the adaptive engine genuinely responds live in-browser: an "always answer match" bot strategy is wrong on the ~62% of trials that aren't matches, and the run correctly ended with difficulty stepped down and copy framed as "eased back... to keep this challenging but doable," not a failure message. Owner: Senior Full-Stack Engineer / Senior UI/UX Designer.
- **[Working Memory] Adaptive Complex Span exercise logic** — `packages/cognitive-engine`'s `ComplexSpanTask`, mirroring `NBackTask`'s pattern (wraps `RollingWindowAdaptiveEngine`; difficulty *is* set size here, same as N is difficulty for N-Back). Implements the real complex-span paradigm, not a plain digit/letter span: each to-be-remembered letter is preceded by a simple arithmetic true/false "processing" check (the mechanism that distinguishes complex span from simple span by preventing pure rehearsal), scored separately from recall accuracy. Full serial recall is scored position-by-position (`correctPositions`) with a strict `fullyCorrect` flag driving the adaptive engine — a reordered or short recall is never silently treated as a pass. 14 tests: set lifecycle (`nextMemoryItem`/`submitRecall` throw outside an active set), no letter repeats within one set (traced by hand with a deterministic injected `random`), recall scoring for perfect/reordered/short sequences, arithmetic-item generation for true/false/subtraction-operand-swap cases, and difficulty confirmed unchanged after one failed set but stepping down/up by exactly one after a full window of failures/successes — proving it defers to the adaptive engine rather than reimplementing adjustment. One real bug caught by the test suite itself before this ever ran in a browser: `nextProcessingItem()` was unconditionally consuming a `random()` call for the wrong-answer perturbation even on branches where the item was supposed to be shown correct, desyncing the deterministic test sequences; fixed by moving that consumption inside the `!showCorrect` branch. Owner: Senior Full-Stack Engineer / Psychometrics Specialist.
- **[Working Memory] Complex Span exercise screen (apps/web)** — `/train/complex-span`, auth-gated, `<ComplexSpanExercise>` running 5 real sets against `ComplexSpanTask`, alternating the true/false processing step with letter presentation, then a recall step, per set. `prototype/ExerciseComplexSpan.dc.html` only shows one representative moment of the task (the processing step) — it has no recall-phase UI at all — so the recall screen (a row of recall slots plus a tap-to-select letter grid) is new, adapted from the numpad-style tap interaction already established in `prototype/ExerciseDigitSpan.dc.html` rather than inventing a third interaction pattern, and documented as a departure in the component's own header comment. Results screen follows the same honest, non-overclaiming pattern as N-Back's: span-size change and processing accuracy are reported as adaptive-engine behavior, not an ability claim. **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/complex-span.spec.ts`), against a production build (`next build && next start`) per `CLAUDE.md`'s E2E command — the Claude-in-Chrome extension still isn't the path here, same as N-Back. Unlike the N-Back spec (which deliberately answers every trial "match" to get a plausible-but-imperfect accuracy), this test reads each memory letter as the real UI displays it and taps it back in the same order, so it exercises a genuine correct-recall path through the real `ComplexSpanTask` scoring logic — all 5 sets, one set size step-up, `5/5` perfect sets confirmed on the results screen. Run 3 times in a row with zero failures to build confidence after one transient failure earlier in development (a fast, no-delay run of the test failed once with the recall UI seemingly skipped straight into the next set; adding temporary debug logging to trace phase transitions made it disappear and reproduce cleanly 3/3 afterward, which points to test-side timing sensitivity in the original assertion sequencing rather than a reproduced app defect, but this is flagged here rather than swept under "just flaky" — if this resurfaces, the debug logging pattern that isolated it once is the fastest way back in). Also confirmed no regression in `apps/web/e2e/n-back.spec.ts` re-run against the same build. Owner: Senior Full-Stack Engineer / Senior UI/UX Designer.
- **[Working Memory] Spatial Sequence Recall exercise logic + screen** — `packages/cognitive-engine`'s `SpatialSequenceTask`, mirroring `ComplexSpanTask`'s set-based lifecycle pattern (`startSequence`/`nextSequenceItem`/`submitRecall`) but as genuine *simple* span, not complex span — no secondary processing task, matching how `docs/evidence-review.md` describes the Corsi-block paradigm the "visuospatial-sequence-recall-v0" evidence module covers. Positions are indices into a 3x3 grid (0-8), same convention as `NBackTask`'s `GRID_SIZE` (renamed `SPATIAL_GRID_SIZE` in this module to avoid a re-export collision with n-back's constant). Difficulty *is* sequence length, same pattern as N/set-size in the other two exercises. 12 tests: sequence lifecycle (throws outside an active sequence), no position repeats within one sequence (deterministic injected `random`), grid-bounds check, recall scoring for perfect/reordered/short sequences, and difficulty confirmed unchanged after one failed sequence but stepping down/up by exactly one after a full window of failures/successes. `/train/spatial-sequence` screen (`<SpatialSequenceExercise>`) runs 5 real rounds against the task, adapted from `prototype/ExerciseSpatial.dc.html`'s static single-moment mockup into a full study/recall/feedback phase loop (the prototype only shows one lit-up frame, no phase sequencing) — reusing the mockup's exact domain tag ("SPATIAL MEMORY · SEQUENCE RECALL") and grid-tap interaction. Added the `spatial`/`spatial-soft` color tokens to `apps/web/src/app/globals.css` (light + dark + the `@theme inline` mapping) — they already existed in `packages/design-system/src/tokens.ts`/`prototype/ExerciseSpatial.dc.html` but `apps/web` hadn't needed them until this exercise. Results copy follows the same non-overclaiming pattern as the other two exercises' results screens, honoring the evidence module's claim limit ("must not claim general spatial intelligence... improvement") — framed strictly as sequence-length/adaptive-engine behavior. **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/spatial-sequence.spec.ts`), against a production build per `CLAUDE.md`'s E2E command. Like the Complex Span spec, it reads the sequence as it's actually shown — via a `highlighted-cell` test id echoing the currently-lit grid position — and taps it back in the same order, exercising a genuine correct-recall path through the real scoring logic rather than a guessed tap pattern; ran 3 times total (once solo, twice more via `--repeat-each=2`) with zero failures, and once alongside the full existing suite (all 3 specs green together, confirming no regression in N-Back/Complex Span). One process note from this run, not an app defect: the documented E2E `next start` command needs its env vars passed inline on the command line (as `CLAUDE.md` already shows) rather than relying on the repo-root `.env` — `next start`'s cwd is `apps/web`, which has no local `.env`, so a plain `pnpm exec next start` there silently runs without `AUTH_TRUST_HOST` and fails every auth-gated flow with `UntrustedHost`. Owner: Senior Full-Stack Engineer / Senior UI/UX Designer.
- **[Reading Training] Reading baseline + paced/adaptive reading exercise** — `packages/reading-engine`'s first real implementation: `passages.ts` (5 original, non-copyrighted passages per project_prompt.txt's READING MATERIAL guidance, each with one comprehension question requiring a specific detail or cross-sentence inference, not a guessable one, per its COMPREHENSION QUESTIONS guidance), `reading-efficiency-score.ts`, and `PacedReadingTask` (mirroring `ComplexSpanTask`'s pattern — wraps `RollingWindowAdaptiveEngine`, difficulty *is* target WPM here via a fixed ladder, and "comprehension holds" — the question answered correctly — is the trial signal, per project_prompt.txt's "Difficulty adapts only when comprehension remains adequate"). The comprehension floor (0.70) required real literature grounding before being hardcoded, per this card's own instruction and project_prompt.txt's "do not hardcode an arbitrary threshold without reviewing research": sourced from Betts' (1946) Informal Reading Inventory instructional-level convention (~70% comprehension is the classic reading-assessment boundary between adequate and "frustration level" reading) rather than the Klimovich RCT already in `docs/evidence-review.md` §8, which establishes realistic WPM-gain *size*, not a pass/fail comprehension threshold — both `docs/evidence-review.md` §8 and the `reading-paced-adaptive-v0` entry in `data/evidence-registry.json` were updated with this citation in the same change per CLAUDE.md's evidence-gating-chain rule, and `pnpm --filter @lean-academy/db seed` was re-run to sync it. The score formula itself is continuous at the floor (no cliff) but applies an extra comprehension-ratio penalty below it, so speed degrades faster than linear once comprehension drops — verified by a dedicated test against project_prompt.txt's own worked example (450 WPM/43% comprehension must score below 250 WPM/90%). 16 tests total across the three files. `/train/reading` (`<PacedReadingExercise>`) runs 5 real passages against the task, adapted from `prototype/ExerciseReading.dc.html`'s single-frame mockup into a full reading/question/feedback phase loop with a genuine sweeping visual pace guide (word-chunk highlight advancing at the current target WPM) — the guide is only ever a suggestion; actual WPM is measured from real elapsed time to "I've finished reading," never from the guide's speed. Added the `reading`/`reading-soft` color tokens to `apps/web/src/app/globals.css` (already existed in `packages/design-system`, same gap as the Spatial Sequence card's `spatial` tokens). Results screen always shows WPM and comprehension together, never the combined score alone, per the project's non-negotiable content rule. **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/reading.spec.ts`), against a production build. Like the other exercises' specs, it exercises a genuine correct-comprehension path — reading the actual rendered question prompt back and matching it against the real passage bank to tap the real correct choice, not a guessed pattern; ran 3 times total (once solo, twice more via `--repeat-each=2`) with zero failures, and once alongside the full existing suite (all 4 specs green together). Owner: Senior Full-Stack Engineer / Cognitive Scientist / Senior UI/UX Designer.
- **[Onboarding] Baseline/calibration flow** — implements project_prompt.txt's onboarding sequence exactly: "goals -> available time -> experience level -> short calibration -> recommended level (accept or change) -> first plan." Full-scope build (a scope check surfaced that no exercise persisted anything to the database at all before this card — everything was client-side/in-memory — so this card also stood up that persistence layer for the first time, not just the onboarding screens). `/onboarding` (`<OnboardingFlow>`) covers all 4 prototype screens (`OnboardingGoals`/`Onboarding`/`OnboardingExperience`/`Baseline.dc.html`) as one route with internal phase state rather than 4 separate routes — a deliberate departure, documented in the component's own header comment, since nothing needs to touch the database until the very last step. The calibration battery reuses the real `NBackTask`/`ComplexSpanTask`/`SpatialSequenceTask`/`PacedReadingTask` engines already built for the 4 exercises, run briefly (2 spatial rounds, 1 warm-up + 2 scoreable n-back stimuli at fixed N=1, one 3-letter simple-span recall with no processing step, one reading passage) rather than inventing separate scoring logic, matching the mockup's "about 90 seconds, no pass or fail." On completion, `POST /api/onboarding/complete` creates a real `TrainingPlan`, upserts a `DailyGoal`, upserts one `DifficultyState` row per implemented task (a plain-language level the user accepted or overrode — `BEGINNER`/`INTERMEDIATE`/`ADVANCED`/`EXPERT`/`AUTO` — maps to each task's own starting-difficulty number server-side, never showing the user a raw internal `Difficulty` number, per CLAUDE.md's difficulty-display rule), and creates an `AssessmentResult` row (type `BASELINE`). Needed real `TaskDefinition`/`TaskVersion` rows to attach `DifficultyState` to, which didn't exist either — extended `packages/db/prisma/seed.ts` to seed them for the 4 implemented `method` ids (keyed to the same evidence-registry ids). Exported each task's previously-private min/max/default difficulty constants (`NBACK_*`/`COMPLEX_SPAN_*`/`SPATIAL_SEQUENCE_*`/`READING_*`) so the onboarding API route could map a named level to a real per-task number without duplicating magic numbers. One real bug caught before ever running in a browser, by the project's own stricter `eslint-plugin-react-hooks` rules (not a human review): the first draft's four calibration mini-components called `setState`/`performance.now()`/task methods directly in plain render-body functions gated by an `if (!ref.current)` check, and read a ref's `.current` as a prop during render — all real anti-patterns the linter caught (`react-hooks/set-state-in-effect`, `react-hooks/purity`, `react-hooks/refs`), not lint noise; restructured into the single continuous `useEffect` async-loop-with-promise-resolvers shape the other 4 exercises already use, rather than suppressing the lints. **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/onboarding.spec.ts`) — the only exercise/flow spec so far that also asserts the real DB rows land (a `TrainingPlan`, a `DailyGoal`, all 4 `DifficultyState` rows, an `AssessmentResult`), not just the client-side scoring the other specs check. Caught and fixed a real test-design bug along the way: the calibration's simplified letter-memory display never actually hides between letters (no processing step to gate it, unlike the real `ComplexSpanExercise`), so a first-draft `toBeVisible`/`toBeHidden`-per-item check was structurally wrong regardless of timing; fixed by reusing the same continuous-polling capture technique already proven for the Spatial Sequence spec. Ran 3 times total (once solo, twice more via `--repeat-each=2`) with zero failures, and once alongside the full existing suite (all 5 specs green together). One known, deliberate simplification: onboarding is an opt-in "Start onboarding" link on the home page, not a mandatory post-signup redirect — forcing that would need an "already onboarded" check to avoid re-onboarding existing users and would change the signup e2e specs' current "signup lands on home" assumption, left as separate future scope. Owner: Senior Full-Stack Engineer / Psychometrics Specialist / Senior UI/UX Designer.
- **[Daily Training] Session orchestration** — strings exercises together without returning to a menu between them (Home -> Exercise -> transition -> Exercise -> ... -> Session Complete), per prototype/Home.dc.html and SessionComplete.dc.html. Full-scope build (a scope check confirmed the user wanted real per-trial persistence during sessions, not just a session-level summary — see the Baseline/calibration card's own scope note; this is the second card in a row where the fuller option was chosen). Added `apps/web/src/lib/session-types.ts` (`SessionModeProps`/`ExerciseSessionOutcome`/`TrialInput`) and refactored all 4 existing exercise components (`NBackExercise`/`ComplexSpanExercise`/`SpatialSequenceExercise`/`PacedReadingExercise`) to accept optional `initialDifficulty`/`onComplete` props — when `onComplete` is given, a component skips its own results screen and hands back real trial-level data instead; when omitted (the existing standalone `/train/<exercise>` routes), behavior is unchanged, so none of the 4 exercises' own e2e specs needed to change. `apps/web/src/lib/todays-training.ts`'s `getTodaysTraining` picks one exercise per domain (WORKING_MEMORY -> READING -> SPATIAL) from the user's real `DifficultyState` rows, alternating across multiple candidates in the same domain (currently only WORKING_MEMORY has two: `adaptive-nback-v0` and `complex-span-v0`) by completed-session count so the same one isn't always picked. `POST /api/training-sessions`, `POST /api/training-sessions/:id/exercises` (creates real `Trial` rows and advances `DifficultyState` per exercise, continuing from wherever the session started rather than resetting), and `POST /api/training-sessions/:id/complete` back a new `<TrainingSessionRunner>` that renders one exercise at a time with a brief "Nice work — next up: X" transition between them, then a `SessionComplete` screen (per-exercise before/after summary rows, matching `SessionComplete.dc.html`). The real Home screen (`apps/web/src/app/page.tsx`, replacing the Phase 2 scaffold) now shows a real "Today's Training" card sourced from `getTodaysTraining` (or an onboarding prompt if the user has no `DifficultyState` yet) — deliberately scoped down from the full mockup: streak counter, weekly activity bar, upcoming-assessment/achievement cards, and the bottom nav are left to their own future Gamification/Progress cards, not faked here. Caught by the project's `eslint-plugin-react-hooks` rules again, not human review: `useRef(Date.now())` as a hook initializer and a plain `Date.now()` call inside the "complete" render branch were both flagged by `react-hooks/purity` (impure calls during render) — fixed by moving both into the effect/event-handler code paths that already existed for exactly this reason. **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/session.spec.ts`) — completes real onboarding first (so real `DifficultyState` rows exist), then drives through a full 3-exercise session end to end, detecting which exercise is actually showing at each step (by its own unique test id) rather than assuming a fixed WORKING_MEMORY exercise, since `getTodaysTraining`'s alternation isn't guaranteed to pick the same one; asserts the real DB rows afterward (`TrainingSession` status `COMPLETED` with a positive duration, `Trial` rows present across exactly 3 distinct `TaskVersion`s). Ran twice solo (~1.5 min each, since a full session chains 3 full-length exercises) plus once alongside the full existing suite (all 6 specs green together). Owner: Senior Full-Stack Engineer / Senior UI/UX Designer.
- **[Progress] Progress page v0** — prototype/Progress.dc.html; Trained / Similar tasks / Broader transfer tabs, reading its own dedicated 30-day WPM/comprehension card. All real: `apps/web/src/lib/progress-data.ts`'s `getProgressData` reads directly from the `Trial`/`DifficultyState` rows Session orchestration started writing — a user who has only tried exercises via their standalone `/train/<exercise>` routes (which never persist, by design) shows up honestly as "nothing trained yet," not as having invisible progress. The "Trained" tab excludes reading (it gets the dedicated card instead, matching the mockup's own structure), shows each task's real evidence-level badge (from `data/evidence-registry.json` via `packages/evidence`'s `findModule`, not hand-typed), a plain-language before/after line formatted per task's own metric (`Level X → Level Y over N sessions` for N-Back/Spatial Sequence, `X-item → Y-item best span` for Complex Span, matching the mockup's own per-task phrasing), and a progress bar computed from each task's real min/max range — extracted the per-task bounds lookup the onboarding route had inline into a shared `apps/web/src/lib/task-bounds.ts` rather than duplicating it a second time. Two honest, deliberate departures from the mockup's specific example content (documented in `ProgressView.tsx`'s header comment): "Similar tasks" shows a real "no near-transfer assessment yet" state instead of the mockup's fabricated result narrative, since no near-transfer assessment mechanic exists yet (only the onboarding `BASELINE` calibration does); "Broader transfer" is copied close to verbatim since it was already an honest static disclaimer with nothing to make more real. **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/progress.spec.ts`) — confirms the empty/"calibrated, no sessions yet" state right after onboarding (before any Trial rows exist), then runs two full training sessions and confirms the reading WPM/comprehension comparison card and real per-task progress lines appear, cross-checked against the real `Trial` rows in Postgres directly. Caught one genuine, minor architectural nuance while writing the test, not a bug: `TrainingSessionRunner`'s exercise/completion POSTs are deliberately fire-and-forget (so the UI never blocks on a network round-trip between exercises), so which `WORKING_MEMORY` candidate (N-Back vs. Complex Span) a second same-day session picks isn't strictly guaranteed to alternate if the prior session's completion write hasn't landed yet when the next one's `getTodaysTraining` query runs — harmless (both are real, honest data either way) and left as-is rather than adding synchronous blocking for a same-day edge case. Ran once alongside the full existing suite (all 7 specs green together). Owner: Senior Full-Stack Engineer / Senior UI/UX Designer.
- **[Science/Evidence] Science page v0** — prototype/Science.dc.html; renders directly from `data/evidence-registry.json` via `packages/evidence`'s loader, per the Evidence Registry Loader & Catalog Gate card's Done note — no separate hand-maintained copy. `apps/web/src/lib/science-data.ts`'s `getScienceData` maps every registry module straight into the page; the one thing the registry doesn't know — which methods are actually implemented in the app yet ("X in your training" in the mockup) — is cross-referenced from the real `TaskDefinition` rows `packages/db/prisma/seed.ts` already writes for exactly the 4 implemented exercises, not a second hardcoded list. `ScienceView` needed no client-side state (no tabs, unlike `ProgressView`), so it's a plain server component. Two honest departures from the mockup, both documented in the component's header comment: the "Tap one to see what it trains..." intro copy implies a per-module detail drill-down screen that doesn't exist yet, so the v0 intro copy doesn't promise that and rows aren't rendered as tappable rather than shipping dead taps; the excluded-methods footer note is fully data-driven (the real `displayName`s of `productionApproved: false` modules) instead of the mockup's specific hand-typed example text. **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/science.spec.ts`) — loads the registry directly in the test too (`loadEvidenceRegistry()`) and asserts the page's stats card and every approved/excluded module against it, so a future registry change that isn't reflected on the page would fail this test rather than only being caught by eye. Ran once alongside the full existing suite (all 8 specs green together). **This was the last card in Phase 3's Ready/Backlog — see the note above this Done section.** Owner: Senior Full-Stack Engineer / Cognitive Scientist.
- **[UX Polish] Accessibility audit (Phase 4, first card)** — `docs/testing.md` calls for "automated (axe-core or equivalent) in CI plus manual keyboard-navigation and screen-reader spot checks per release"; this is the first real pass. Added `@axe-core/playwright` and a new `apps/web/e2e/accessibility.spec.ts` scanning every real, built screen (login, signup, home, onboarding, progress, science, and all 4 exercises mid-run) plus a manual keyboard-navigation check, per `docs/design-system.md`'s Accessibility rules. This was a genuine audit, not a rubber stamp — it found and fixed real bugs across the app, not just in new code: (1) **`--color-text-3` failed WCAG AA contrast against the app's own background** (measured ratio 3.43-4.46, needs 4.5) — the design token itself was wrong, not any one usage of it; darkened it (light mode only) in both `apps/web/src/app/globals.css` and `packages/design-system/src/tokens.ts` (kept in sync per `CLAUDE.md`'s Monorepo layout note; `prototype/Styleguide.dc.html` still has the old value and needs a follow-up pass through the `design` skill to reseed — not done here since that's a heavier action than a token-number correction warranted on its own). (2) **Same failure in the `wm`/`spatial`/`reading` domain colors**, both as text-on-background and as white button-text-on-domain-colored-background (two directions that turned out to both improve from the same fix, not conflicting constraints as first assumed) — computed exact target values with a scratch OKLCH-contrast calculator (validated against axe's own real measurements first) rather than guessing through rebuild cycles, then verified empirically that all 4 exercises' domain tags/buttons pass, not just the one axe happened to flag first. (3) **Several selection controls across onboarding and the reading exercises were plain `<div onClick>` elements — completely unreachable by keyboard or screen reader** (`OnboardingFlow`'s shared `OptionRow`, used by all of goals/time/experience/difficulty; its calibration reading-question answer choices; `PacedReadingExercise`'s real answer choices) — converted to real `<button role="radio" aria-checked>` elements inside `role="radiogroup"` containers, verified with a real keyboard-only interaction test (focus + Enter, not just an axe scan, since axe can't detect "this is a click-only div" on its own). (4) **No page had a level-one heading** — added a real `<h1>` to `AuthForm`, each `OnboardingFlow` step, `ProgressView`, `ScienceView`, and a screen-reader-only `<h1>` to each exercise's live view (each already has a visible "Exercise complete" heading on its results screen, now also a real `<h1>` there). (5) `SpatialSequenceExercise`'s empty grid-cell buttons had **no accessible name at all** during the study phase — added `aria-label`. (6) A leftover Phase 2 scaffold `text-neutral-500` class on the Home page (never migrated to the design-token system) was part of the same contrast failure — switched to `text-text-3`. **All 6 findings were fixed, not just reported**, and re-verified: the full accessibility spec (7 tests) and the complete existing e2e suite (15 tests total) both pass together. Also added a `mobile` Playwright project (`devices["iPhone 13"]`, scoped via `testMatch` to a not-yet-written `responsive.spec.ts` so it doesn't double-run every other spec) as prep for the next Phase 4 card. Owner: Senior Full-Stack Engineer / Senior UI/UX Designer.
- **[UX Polish] Mobile-responsive-web pass** — every screen before this had only ever been checked at Playwright's Desktop Chrome default viewport. Wrote `apps/web/e2e/responsive.spec.ts`, which runs under both the `chromium` (desktop, ~1280px) and `mobile` (iPhone 13, 390px) Playwright projects using the same assertions at both real widths, rather than one hardcoded size: no horizontal overflow (`document.documentElement.scrollWidth` vs `clientWidth`) and real tap-target sizing (WCAG 2.2 AA's 24×24 CSS px "Target Size (Minimum)" — the closest published standard, since no specific number is given anywhere in this project's own docs) across login, signup, home, onboarding, progress, science, and an exercise mid-run. Needed a real WebKit browser install (`pnpm exec playwright install webkit`) — the `mobile` project's `devices["iPhone 13"]` preset correctly defaults to WebKit (Safari's engine) for realistic iOS emulation, not Chromium. **Genuine audit outcome, not a rubber stamp either way: this one found zero real bugs** — the app's fixed `max-w-[390px]` phone-card layout already handles both viewport extremes correctly (no overflow, tap targets already sized well above the 24px floor, and the card centers as an intentional-looking column at desktop width rather than looking broken). Verified: both projects' `responsive.spec.ts` tests pass, and the full existing suite still passes (one apparent failure in `progress.spec.ts` under a 6-worker fully-parallel run turned out to be worker resource contention on this machine, not a regression — confirmed by re-running it alone, where it passed in the same ~2.3 minutes it always has). Owner: Senior Full-Stack Engineer / Senior UI/UX Designer.
- **[UX Polish] Animation/feedback pass** — implements `docs/design-system.md`'s Motion section: "a small set of named durations/easings... rather than per-component timing values." Added `--duration-micro`/`--duration-transition`/`--duration-celebration` and `--ease-standard`/`--ease-celebration` to `apps/web/src/app/globals.css` (exposed as real Tailwind utilities — `duration-micro`, `ease-celebration`, etc. — via `@theme inline`, not just CSS variables) and mirrored them in `packages/design-system/src/tokens.ts`'s new `motionTokens` export, per `CLAUDE.md`'s three-places-in-sync rule. Added a global `prefers-reduced-motion: reduce` rule collapsing every CSS transition/animation to ~0 while leaving the underlying JS-timed task pacing (`performance.now()`/`setTimeout`) completely untouched — the rule is pure CSS and only targets `animation-duration`/`transition-duration`, so it structurally cannot touch the measured stimulus timing the Animation rules explicitly protect ("decorative animation must never run during a timed stimulus window"). Consistent tap-press feedback (`transition-transform duration-micro active:scale-*`, previously only on `NBackExercise`'s respond button) added across every previously-bare interactive control: `OnboardingFlow`'s shared `OptionRow`/`ContinueButton` and all of its bespoke calibration buttons, both remaining exercises' recall/submit buttons, `AuthForm`'s submit button, and `ProgressView`'s tabs. Added a subtle `animate-fade-in-up` entrance (a new `data-testid="results-screen"` on all 5 results/Session-Complete screens) and, per the Animation rules' "scale to the significance of the event... do not exaggerate scientifically meaningless improvements," a `animate-celebration-pop` accent on the difficulty-delta stat *only* when it actually increased (`endDifficulty > startDifficulty`, already-real adaptive-engine data — no fabricated "personal best" mechanic was invented, since no historical-best tracking exists yet). **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/motion.spec.ts`) — Claude-in-Chrome wasn't connected this session, so this is the CLAUDE.md-mandated fallback, not a skip: checks the real computed `animationDuration`/`transitionDuration` (not just that a className string is present, which would pass even if a Tailwind theme-key typo silently no-op'd the utility), confirms it's non-zero by default, and confirms `page.emulateMedia({ reducedMotion: "reduce" })` collapses it to sub-millisecond. Ran alongside the full existing suite (all specs green; one `onboarding.spec.ts` timeout under 6-worker full parallelism was confirmed to be resource contention, not a regression, by re-running it alone in 14s). Owner: Senior UI/UX Designer / Senior Full-Stack Engineer.
- **[UX Polish] Performance optimization** — establishes the baseline `docs/development-plan.md`'s Phase 4 exit criteria calls for ("Core Web Vitals-equivalent performance targets met"); none existed before this. No specific numeric target exists anywhere in this project's own docs (checked `docs/product-requirements.md`, `docs/testing.md`), so `apps/web/e2e/performance.spec.ts` uses Google's published Core Web Vitals "Good" thresholds (LCP ≤ 2.5s, CLS ≤ 0.1) — the same kind of resolution as the responsive pass's WCAG 2.2 tap-target-size choice — measured via real `PerformanceObserver` entries in a live Chromium browser, not a synthetic estimate. Also tracks the two targets `docs/testing.md` names beyond page load: route-transition time (home → an exercise, real `Date.now()` deltas around a real click) and exercise-startup time (folded into the same measurement). **A real investigation that changed conclusion under verification, reported honestly rather than as a clean win:** a static inspection of `.next/static/chunks/*.js` found a 112KB `core-js` legacy-browser polyfill bundle referenced in every route's build manifest, with no explicit `browserslist` config anywhere in the repo (Next/browserslist falls back to a broad implicit default that includes old browsers needing heavy ES-feature polyfills) — looked like a real, fixable finding, so added an explicit `browserslist` field to `apps/web/package.json` targeting modern evergreen browsers only (`"defaults and supports es6-module"`) or maintained Node versions, matching this app's own "modern, fast" brand direction (`docs/design-system.md`) with no stated requirement to support legacy browsers anywhere in the spec. Rebuilding showed the chunk's size **didn't change at all** — before concluding the fix worked, checked with a live network trace whether that chunk is actually *fetched* by a real page load rather than just present in a manifest (a route's build-manifest lists every chunk reachable through any code path, not only what a cold load requests) — real measurement showed **0 core-js bytes were ever fetched**, before or after the config change; the chunk exists on disk but was never live traffic. Kept the `browserslist` addition anyway as a legitimate, low-cost hardening (explicit target beats an implicit fallback regardless), but the honest conclusion is "investigated a plausible lead, it wasn't real, verified with a network trace rather than taking the static analysis at face value" — not "fixed a bug." All 5 `performance.spec.ts` checks pass (LCP/CLS on 5 pages, one route transition, total JS payload, the core-js check) and the full existing suite (27 tests) passes alongside it. One important caveat documented in the spec file itself: all measurements are on localhost with no network throttling, so these numbers establish a *local* baseline, not a real-world one — a follow-up with Playwright's/CDP's network-conditions emulation would be needed before trusting these thresholds under real mobile-network conditions. Owner: Senior Full-Stack Engineer.
- **[Daily Training] Streaks (with protection, non-manipulative copy)** — Phase 5's first card. `Streak.currentStreakDays`/`longestStreakDays`/`lastActiveDate` existed in the schema but nothing wrote to them; a new `streakFreezesAvailable` field (migration `20260914180419_add_streak_freeze`) makes the protection mechanic real rather than a stub. `apps/web/src/lib/streak.ts`'s `recordActiveDayForStreak` is called from `POST /api/training-sessions/:id/complete` — the one real completion trigger `TrainingSessionRunner` hits, so a streak only advances for an actual completed session, never a standalone unpersisted exercise run, consistent with how Progress already treats standalone exercises. Day-boundary rule is UTC calendar day, documented explicitly in the code as a v0 simplification since `User` has no timezone field yet. Streak protection is a genuine mechanic, not just soft copy: a new streak starts with 1 freeze, one more is earned (capped at 2) every 7 consecutive active days, and a freeze silently bridges exactly one missed day — two or more missed days always resets the streak even with freezes banked, so protection can't be stacked into indefinite immunity. Missed-day copy on the Home page ("N-day streak — ready to continue your training?") reuses `project_prompt.txt`'s own approved example verbatim rather than inventing new copy, and nothing resembling its banned example ("You are losing brain power!") appears anywhere. The Home page now shows the real streak indicator that was deliberately left out when it was first built during Session orchestration, and the Session Complete screen shows the real post-session streak update, including an honest note when a freeze was spent. `apps/web/e2e/streaks.spec.ts` verifies both the real HTTP trigger end-to-end in a live browser (signup → complete a session → real `Streak` row → visible on Home) and, via direct calls to the same production `recordActiveDayForStreak`/`getStreakStatus` functions against real Postgres with a controlled `now`, the full multi-day timeline: daily increments, freeze earned at day 7, freeze spent on exactly one missed day, and a reset on two missed days — a browser can't fast-forward real calendar days, so this is the same real function and real DB, just not driven through `page.goto`. Full existing suite (27 tests, chromium project) passes alongside the 2 new ones. Owner: Senior Full-Stack Engineer.
- **[Gamification] Achievements** — Phase 5's second card. Seeded the exact 11 real, checkable milestones `prototype/Achievements.dc.html` specifies via a new shared `packages/db/src/achievement-catalog.ts` (imported by both `prisma/seed.ts` and the trigger logic, so a typo'd key can't silently mean an achievement is never awarded or never seeded). Real trigger points, all at existing completion routes rather than a new polling job: `POST /api/training-sessions/:id/complete` (First Session, 10/30/100 Sessions via a real `TrainingSession` count, First Week via the real `Streak` reaching 7, and a fixed v0 weekly-session target of 5 — documented as a placeholder pending the real per-user target the Weekly Challenges backlog card will add), `POST /api/training-sessions/:id/exercises` (Working Memory Level 5, Personal Best, the Reading Efficiency Milestone, and "maintained 90% comprehension at a new pace"), and the onboarding-completion route (Completed first assessment). Three real design decisions the card's acceptance criteria flagged as open, now resolved and documented in `apps/web/src/lib/achievements.ts`: (1) **Working Memory Level 5** awards on *either* N-Back or Complex Span reaching the same raw difficulty number 5 — an explicit choice, not a hidden default, since the two tasks' ranges aren't otherwise comparable; (2) **Personal Best** is real, not vibes — it compares a task's new difficulty against the actual historical max from `Trial` rows (excluding the current session), so it only fires on a genuine improvement over real history, never on a first attempt with nothing to beat; a new `UserAchievement.metadata` JSON field (migration `20260914181756_add_user_achievement_metadata`) records which task earned it so the UI can show "Personal Best — Adaptive N-Back" the way the prototype shows "Personal Best — Spatial Sequence"; (3) **the two reading achievements are independently checkable**, not the same condition twice — the Milestone fires the first time pace exceeds the starting default ever, while the comprehension one requires a real pace increase *in that specific session* combined with that same session's own comprehension staying at/above 90%, computed straight from the `trials` already in the request body. Every title/description is spot-checked against `project_prompt.txt`'s SCIENTIFIC PROGRESS LANGUAGE section — none resembles its "10% smarter" anti-example. New real UI: `/achievements` (`AchievementsView`, linked from Home) shows a real "N of 11 earned" header and, matching the prototype exactly, progress bars only where one is meaningful (10/30/100 Sessions, the weekly target) — every other achievement is a plain earned/not-earned line, since the prototype itself never fakes a progress bar for something with no natural running count. `apps/web/e2e/achievements.spec.ts` verifies both a real browser flow (signup → real onboarding-completion and session-completion API calls → real `UserAchievement` rows → visible on `/achievements`) and, via direct calls to the same production `checkSessionCompletionAchievements`/`checkExerciseAchievements` functions against real Postgres, every milestone that would otherwise need dozens of real sessions or exercise runs driven through the UI. Full suite (31 tests, chromium project) passes. Owner: Senior Full-Stack Engineer.
- **[Gamification] XP + Training Level** — Phase 5's third card. A new `XpEntry` model (migration `20260914191125_add_xp_entry`) is a real award ledger, not a mutable running total — Total XP is always a live aggregate over real rows, so the anti-grind cap (see below) can be computed honestly from real history rather than trusting a counter that could drift, and there's a real audit trail for how a Training Level was reached. `apps/web/src/lib/xp.ts` implements all four real XP sources `project_prompt.txt`'s XP AND LEVELS section names: completing recommended training (20 XP per completed `TrainingSession`), consistency (a real bonus scaling with the actual `Streak.currentStreakDays` from the same completion, 2 XP/day capped at a 10-day streak — reuses the Streaks card's data, no new instrumentation), trying new training domains (15 XP the first time a user's `Trial` history for a given `TaskVersion` is genuinely empty, checked the same way the Achievements card's Personal Best does), and genuine task milestones (25 XP hooked directly into `achievements.ts`'s `awardAchievementOnce` — which had to change from an upsert to a real find-then-create specifically so it can tell a genuinely new award from an already-earned no-op, since only the former should pay XP). **A real, testable anti-grind cap**, per `project_prompt.txt`'s own explicit instruction ("avoid rewarding endless repetitive grinding... cap or reduce XP from excessive training"): a hard 150 XP/UTC-day ceiling computed from real `XpEntry` rows created that day, clamping (never negative) any award that would exceed it — verified in `apps/web/e2e/xp.spec.ts` by actually granting repeated large awards in one simulated day and confirming the total never exceeds the cap and a capped-out award writes no row at all. **Training Level** is a documented, real formula (not an opaque number): a triangular curve where level *L* costs a cumulative 100·(L-1)·L/2 XP, so each level costs 100 more than the last — shown on the Home page as "Training Level N" next to the streak indicator (matching `prototype/Home.dc.html`'s "8-day streak · Training Level 12" layout exactly) and never as anything resembling "Intelligence Level," per `project_prompt.txt`'s explicit instruction. The Session Complete screen shows the real "+N XP" earned that session, with a celebration-pop moment (reusing the Animation/feedback pass's existing infra) only on an actual level-up. One real bug caught before this was called done, not left for a test to stumble on: the first draft never stamped a new `XpEntry`'s `createdAt` with the function's own `now` parameter, so it silently fell back to the real wall-clock time — harmless in production (where `now` is always "now" anyway) but it broke the whole point of `now` being an explicit, testable parameter, and a real day-boundary test caught it immediately (a "new day resets the cap" assertion failed because the previous day's awards were still real-clock-stamped as "today") — fixed by passing `createdAt: now` through explicitly, the same lesson `apps/web/src/lib/streak.ts` already encoded for `lastActiveDate`. `apps/web/e2e/xp.spec.ts` (7 tests, each real scenario using its own dedicated user to avoid any cross-test shared-state coupling) verifies the real browser trigger end-to-end, the level formula, the consistency-bonus cap, the anti-grind cap, real level-up detection, achievement-XP-awarded-exactly-once, and new-domain-bonus-awarded-exactly-once. Full suite (38 tests, chromium project) passes. Owner: Senior Full-Stack Engineer.
- **[Gamification] Personal bests** — Phase 5's fourth card. No new table, per the card's own dependency note — computed at query time from real `Trial`/`DifficultyState` data, the same way `apps/web/src/lib/progress-data.ts` derives everything else, in a new shared `apps/web/src/lib/personal-bests.ts` module. **A real, documented refinement over the Achievements card's original one-time badge, not just a new page around the same logic**: reading's personal best previously (in `achievements.ts`) compared the same raw target-WPM-ladder-index metric used for every other task, which meant a faster pace with worse comprehension could count as an "improvement" — that's now fixed everywhere by having both the Achievements "Personal Best" check and this card's real-time surfacing share one function (`checkNewPersonalBest`), which for reading compares the real Reading Efficiency Score (WPM combined with comprehension) instead. Verified directly, not assumed: `apps/web/e2e/personal-bests.spec.ts` constructs a session with a real, faster WPM but failed comprehension and confirms it is *not* flagged as a personal best, then confirms a real slower-but-comprehended session correctly is. Session Complete now shows a real, honest "🏆 New personal best" tag on the exact exercise row that earned it (not a generic session-wide banner, since a session can span multiple domains and only some might set a real record) with a celebration-pop moment, and the Progress page's Trained-tab cards and reading card both show the user's real current best per task — for reading, always the real WPM/comprehension pair from whichever session scored highest, never a bare composite number, per the project's non-negotiable WPM-must-carry-comprehension rule. One deliberate scope interpretation, documented rather than assumed silently: the card's "browsable history" language is implemented as "your current real best per task, viewable any time" (a genuine improvement over the achievement's one-time-ever badge), not a full timeline of every past record broken — the latter would need new schema and pushes this well past the card's own S/M complexity estimate; revisit as a separate card if a real timeline view is wanted later. **A real regression caught and fixed before this was called done**: adding the new reading personal-best text to the Progress page broke `progress.spec.ts`'s existing `getByText(/WPM/)` assertion (now ambiguous between the original PACE trend's "WPM" label and the new "X WPM at Y% comprehension" text) — fixed by making that assertion exact-match, and a real second full-suite run confirmed the fix. Full suite (43 tests, chromium project) passes; one `onboarding.spec.ts` timeout under the 6-worker fully-parallel run was confirmed to be machine resource contention, not a regression, by re-running it alone (14s, comfortably under its 90s timeout) — the same pattern already documented on this project's `progress.spec.ts`/`onboarding.spec.ts` entries earlier in this file. Owner: Senior Full-Stack Engineer.
- **[Gamification] Weekly challenges** — Phase 5's fifth and last card. The `Challenge`/`ChallengeProgress` schema existed but nothing created challenges or updated progress before this; added one schema field (`Challenge.slug`, migration `20260915120000_add_challenge_slug`, `@@unique([slug, startsAt])`) so weekly generation has a stable, idempotent identity for "this template, this week" without parsing the opaque `criteria` JSON. New `apps/web/src/lib/weekly-challenges.ts` is real rotation/generation, not a static list: every real UTC week (Monday 00:00 UTC, the same boundary `streak.ts`/`achievements.ts` already use) lazily gets its own 5 `Challenge` rows the first time anything touches that week — no cron infrastructure exists in this project, so lazy creation on the real trigger points is the honest mechanism rather than inventing a scheduler. The 5 challenges are `project_prompt.txt`'s own WEEKLY CHALLENGES examples verbatim, not invented ones, and every week gets the same fixed 5 rather than a randomized subset — a deliberate choice matching `project_prompt.txt`'s own explicit warning ("avoid challenges encouraging excessive usage"), since a fixed, predictable set is easier to keep non-manipulative than an escalating or shifting one. Progress is genuinely real, not fabricated: `syncWeeklyChallengeProgress`, called from all three real trigger points (`POST /api/training-sessions/:id/complete`, `POST /api/training-sessions/:id/exercises`, and the onboarding-completion route), recomputes "complete N sessions," "train N domains," and "complete an assessment" straight from real `TrainingSession`/`Trial`/`AssessmentResult` rows every call — the same "aggregate over real rows, never a hand-incremented counter" pattern `xp.ts`'s `getTotalXp` and `personal-bests.ts` already established — with one deliberate, documented exception: "beat a personal record" is recorded as a real event at the exact moment `achievements.ts`'s `checkExerciseAchievements` already determines a genuine new best (its own `newPersonalBest` return value), rather than re-deriving "was any trial this week a new all-time best" from scratch on every sync call. **A real, not cosmetic, fix for a pre-existing inconsistency**: `achievements.ts`'s "weekly-sessions-complete" milestone previously hardcoded its own separate placeholder target of 5 sessions, one card's own comment already flagged as "a fixed v0 default standing in until a real per-user weekly target exists" — that placeholder and this card's real `complete_sessions` challenge (`project_prompt.txt`'s own example: "Complete 4 sessions") now share one real constant (`WEEKLY_SESSION_COUNT_TARGET = 4`, defined once in `weekly-challenges.ts`), fixing a genuine 5-vs-4 drift between two features that were supposed to mean the same thing, not just silencing the placeholder comment. New `/challenges` page (`ChallengesView`, linked from Home next to Achievements) — no `prototype/*.dc.html` artboard exists for this screen (checked), so it was built to match `AchievementsView.tsx`'s established visual pattern (same card shell, same progress-bar row layout) rather than inventing a new one, documented as a departure in the component's own header comment. **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/weekly-challenges.spec.ts`, 4 tests) against a production build: a real signup → onboarding-completion → real training-session-completion flow confirms the assessment and session-count challenges genuinely advance and appear on `/challenges`, plus direct calls to the same production `syncWeeklyChallengeProgress` function against real Postgres for scenarios that would need many real sessions across real domains to reach through the UI alone — 4 real completed sessions across the 3 real implemented domains (working memory, spatial, reading) genuinely complete both the session-count and domain-variety challenges together, a real sub-70%-comprehension reading session correctly does *not* complete the reading-comprehension challenge (only a real ≥70% one does, reusing the same Betts-1946 floor `reading-engine`'s `COMPREHENSION_FLOOR` already defines), and the personal-record challenge is confirmed to stay completed once earned even after a later sync call that didn't itself beat a record (monotonic, matching every other one-way-completable milestone in this codebase). Full suite (47 tests, chromium project) passes together — one pre-existing flake in `n-back.spec.ts` (a strict-mode Playwright locator matching two "ACCURACY" elements on the results screen) surfaced once under this run and is unrelated to this card (no N-Back code touched here); it passed cleanly every other time it ran, alone and alongside this change, matching this file's own established pattern of documenting isolated flakes rather than either hiding them or blocking on them. **Phase 5 (Engagement) is now fully complete.** Owner: Senior Full-Stack Engineer.
- **[Science/Evidence] Real psychometric scoring library** — Phase 6's first card, replacing `packages/psychometrics`'s `PSYCHOMETRICS_PLACEHOLDER` stub with real functions. Scoped exactly to the two real gaps the card named, not the full eventual set the old placeholder comment listed ("switch cost" etc. gets added when a real future card needs it, not speculatively here): `calculateDPrime`/`calculateSignalDetectionRates` (`signal-detection.ts`) and `calculateProportionConfidenceInterval` (`confidence-interval.ts`), both built on a shared `inverseNormalCdf` (`inverse-normal-cdf.ts`) implementing Peter Acklam's rational approximation to the standard-normal probit function, since both d-prime (`z(hit rate) - z(false-alarm rate)`, Green & Swets 1966) and a Wilson-score confidence interval (Wilson 1927) need it. Every function cites its accepted formula in a comment, per the card's own requirement and `docs/testing.md`'s "scientific-calculation tests get extra scrutiny" rule. Two real correctness details, not just the textbook formulas: (1) hit rate / false-alarm rate get the standard 1/(2N) extreme-proportion correction (Hautus 1995) before feeding into d-prime — without it, a perfect or zero rate (exactly the best- and worst-performing real users) sends `z()` to +/-Infinity; (2) the confidence interval uses the Wilson score interval rather than the simpler normal/Wald interval specifically because it stays bounded in [0,1] and keeps real coverage accurate at the small trial counts a short assessment battery actually produces, and it doubles as the "span score" confidence interval the card also named — there's no separate accepted "span CI" formula to invent one for, since a span-staircase assessment's own uncertainty is itself a proportion-correct measure. N-Back's trials already carry real hit/miss/falseAlarm/correctRejection classifications in `Trial.metadata` (`apps/web/src/components/NBackExercise.tsx`), confirming the card's premise that this can score real existing data, not require new instrumentation — though wiring `calculateDPrime` into any real N-Back result display is explicitly left to a future card, not done here. 20 unit tests across 3 files, including the standard textbook worked examples this kind of calculation gets checked against rather than just internal consistency: Stanislaw & Todorov's (1999) reference d-prime example (H=0.75, FA=0.25 → d′≈1.349) and the commonly-cited Wilson-interval worked example (50/100 at 95% → [0.404, 0.596]). **No real browser verification for this card** — it's a pure calculation library with no UI or API route touching it yet (matching how the original Adaptive Engine Skeleton card was verified), so per the Definition of Done's "state plainly what was and wasn't actually exercised": nothing here was exercised in a browser, only in Vitest against known reference values; real UI/API exposure is exactly what the two near-transfer assessment cards below build next. Full unit-test suite (89 tests across every package with tests: 20 new here, plus adaptive-engine/evidence/reading-engine/cognitive-engine's existing 69) and the full `apps/web` build/typecheck/lint all pass; `apps/web`'s own Playwright suite (47 tests) is unaffected since nothing in `apps/web` calls this package yet. Owner: Data Engineer / Psychometrics Specialist.
- **[Authentication] Fix: credentials sign-in failing right after registration under `next dev`** — root-caused, not just described, this time. Confirmed by network trace (`page.on("request"/"response")`) that `signIn()`'s own `getProviders()`→`getCsrfToken()` sequence can race a *separate* concurrent request to Auth.js's shared route handler that also touches the CSRF cookie — `SessionProvider`'s default `refetchOnWindowFocus` (a `visibilitychange` listener) is one real source of that extra request, confirmed by reading `next-auth`'s source directly rather than assuming. An initial attempt to fix this by pre-warming the CSRF cookie in `AuthForm` on mount made things *worse* (React Strict Mode double-invokes that effect too, adding yet another concurrent request) — reverted. The fix that actually works: `apps/web/src/app/providers.tsx` now sets `refetchOnWindowFocus={false}` (removes one real source of the race), and `AuthForm.tsx`'s `signInWithCsrfRaceRetry` retries the credentials sign-in once after a short delay if the first attempt errors — safe regardless of cause, since a genuine wrong password just fails the same way again on retry. Verified empirically, not just reasoned about: an 8-iteration stress test against a freshly cold-started dev server (cleared `.next/dev` cache each time, coldest and most race-prone condition) passed 8/8 — including one run where the underlying race still fired server-side (one `MissingCSRF` in the log) but the retry transparently recovered it. Owner: Senior Full-Stack Engineer.
- **[Working Memory] Near-transfer assessment: Backward Digit Span** — Phase 6's second card (real psychometric scoring library was the first). A genuine near-transfer measure for WORKING_MEMORY, deliberately **not** built on `RollingWindowAdaptiveEngine`: `packages/cognitive-engine`'s new `BackwardDigitSpanAssessment` implements the classic fixed ascending-span staircase directly (increase span by one on a correct trial, retry once at the same span on a single miss, stop after two consecutive misses) per `project_prompt.txt`'s TRAINING VS ASSESSMENT distinction that assessment is "less frequent measurement," not adaptive practice. **Explicitly not a reproduction of the WAIS/WISC Digit Span clinical subtest** — out of scope permanently per `docs/evidence-review.md`'s excluded-domains list — sequences are freshly randomized every run from an injectable `random`, the same "original task, not the clinical instrument" framing already used for `verbal-sequencing-v0`; `docs/evidence-review.md` gained a new §11 documenting this and the real near-transfer citation it relies on (§3's already-reviewed Melby-Lervåg & Hulme finding on backward-span-structure transfer — no new literature search needed, since this is the direct intended use of that existing finding, not a new claim). Two real design decisions the card asked for, resolved and documented rather than left implicit: (1) the confidence interval (`packages/psychometrics`'s real `calculateProportionConfidenceInterval`, now finally wired into a real UI) is computed over the *terminal span level's own trials* specifically, not the whole staircase — matching that function's own docstring, which anticipated exactly this use; (2) the periodic-trigger rule is a concrete, shared 14-real-calendar-day-since-last-result window (`apps/web/src/lib/near-transfer-assessment.ts`'s `getNearTransferAssessmentStatus`), deliberately factored to take an `assessmentName` parameter so the alternate spatial-span card below can reuse it verbatim rather than inventing a second schedule — it gates a "due" label only, never a hard block, since retaking voluntarily is always allowed. New real UI: `/assessments/backward-digit-span` (auth-gated, `<BackwardDigitSpanAssessment>`), reached from a new CTA link on Progress's "Similar tasks" tab (its existing honest "No near-transfer assessment yet" placeholder now offers a real way to change that). The study-phase UI (one digit at a time, timed) is new — `prototype/ExerciseDigitSpan.dc.html` only mocked the numpad recall step, not the memorize step — built following the same one-item-at-a-time study pacing `ComplexSpanExercise`/`SpatialSequenceExercise` already established, and recall reuses that mockup's exact numpad interaction, adapted from letters to digits 0-9. `POST /api/assessments/backward-digit-span` creates a real `Assessment` row (type `NEAR_TRANSFER`, find-or-create like onboarding's `BASELINE` row) and an `AssessmentResult` with a `scoreSummary` carrying the final span, the real confidence interval, and total/final-span correct counts — and hooks into the same `checkFirstAssessmentAchievement`/`syncWeeklyChallengeProgress` calls onboarding's completion route already makes, so this assessment can genuinely complete the "First Assessment" achievement and the weekly assessment challenge, not just its own feature. Results and intro copy were spot-checked against the project's non-negotiable content rules: framed strictly as a working-memory near-transfer measure, explicitly disclaiming IQ/general-intelligence/everyday-memory claims, and the results screen states plainly that a handful of trials at one span length carries real uncertainty rather than presenting the span number as falsely precise. **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/backward-digit-span.spec.ts`) against a production build: reads the digit sequence as it's actually flashed (a `study-digit` test id) and taps it back in genuine reverse order every trial, so the bot succeeds all the way to the ceiling (max span 9) through the real engine and real DB — confirmed the resulting `AssessmentResult.scoreSummary` directly against Postgres (`finalSpan: 9`, a real `1/1` terminal-span record, `confidenceInterval.proportion: 1`), and confirmed a second visit shows the real "last taken"/"next recommended" status text, not a placeholder. One real bug caught before this was called done: the initial `captureStudySequence` test helper called `locator.textContent()` unconditionally, which auto-waits (blocks) indefinitely once the digit element unmounts between phases — deadlocked the very first run at the 90s test timeout; fixed by checking `locator.count()` before reading text, the same defensive pattern `spatial-sequence.spec.ts`'s `highlighted-cell` polling didn't need only because that element is never unmounted. Also added the new page to `accessibility.spec.ts`'s signed-in-screens scan, matching the established "every real, built screen" coverage rule. Full suite (48 tests, chromium project) and the full unit-test suite (98 tests across every package) both pass alongside this change; root `build`/`typecheck`/`lint` all pass too. The "Wire Similar Cognitive Tasks to real near-transfer data" backlog card (showing this result on the Progress page itself) is deliberately left for its own card, unblocked now but still waiting on the alternate spatial-span assessment per its own stated dependency. Owner: Senior Full-Stack Engineer / Psychometrics Specialist.
- **[Working Memory] Near-transfer assessment: alternate spatial-span variant** — Phase 6's third card, the SPATIAL-domain twin of Backward Digit Span. `packages/cognitive-engine`'s new `BackwardSpatialSpanAssessment` reuses the exact same fixed ascending-span staircase shape (increase span on a correct trial, retry once on a miss, stop after two consecutive misses) on a 3x3 grid instead of digits. **Genuinely distinct from the trained `visuospatial-sequence-recall-v0` exercise, not a repeat with a new label**: the trained `SpatialSequenceTask` is *forward* serial recall, while this assessment requires *backward* recall (tap the lit sequence back in reverse order) — the same real near-transfer distinction Backward Digit Span draws against N-Back/Complex Span, now documented in a new `docs/evidence-review.md` §12 citing §5's already-reviewed Corsi-paradigm near-transfer finding (no new literature search needed, same reasoning as §11). **Explicitly not a reproduction of the clinical Corsi Block-Tapping Test** — the excluded-domains list's clinical-instrument bullet was updated to name it alongside WAIS/WISC Digit Span and OSPAN, since this is the first spatial near-transfer assessment to raise that exact boundary. Deliberately reuses rather than reinvents: the same shared 14-day periodic-trigger helper (`apps/web/src/lib/near-transfer-assessment.ts`, already parameterized by `assessmentName` for exactly this), the same `calculateProportionConfidenceInterval` computed over the terminal span level's trials, and the same find-or-create `Assessment`/`AssessmentResult` persistence shape via a new `POST /api/assessments/backward-spatial-span`. The study/recall grid UI is adapted from `SpatialSequenceExercise.tsx` (same `highlighted-cell`/`grid-cell-N` test ids and tap-order display), not invented fresh — the only behavioral change is asking for the sequence back in reverse. New real UI: `/assessments/backward-spatial-span`, and Progress's "Similar tasks" tab now offers both assessments side by side (`bg-wm`/`bg-spatial` CTA buttons, matching each domain's own color token). Results and intro copy pass the same non-negotiable-content-rule spot-check as Backward Digit Span (near-transfer framing only, explicit IQ/general-intelligence/everyday-memory disclaimer, honest about single-length-trial uncertainty). 12 new unit tests (`packages/cognitive-engine`). **Verified in a real Chromium browser via Playwright** (`apps/web/e2e/backward-spatial-span.spec.ts`) against a production build: reads the grid sequence as it's actually lit (the same `highlighted-cell` polling technique already proven on `spatial-sequence.spec.ts` — this element is never unmounted between phases, so it didn't hit the `study-digit` deadlock bug the digit-span spec's helper needed fixing for) and taps it back in genuine reverse order every trial, running the real engine to the ceiling (grid size 9) and confirming the resulting `AssessmentResult.scoreSummary` directly against Postgres, plus the real "last taken"/"next recommended" status on a second visit. Also added to `accessibility.spec.ts`'s signed-in-screens scan. Full suite (49 tests, chromium project, both near-transfer specs together) and the full unit-test suite (110 tests across every package) both pass alongside this change; root `build`/`typecheck`/`lint` all pass too. Both Phase 6 near-transfer assessment cards are now done — "Wire Similar Cognitive Tasks to real near-transfer data" is fully unblocked. Owner: Senior Full-Stack Engineer / Psychometrics Specialist.
- **[Progress] Wire "Similar Cognitive Tasks" to real near-transfer data** — Phase 6's fourth card, replacing `ProgressView.tsx`'s "No near-transfer assessment yet" placeholder with each real `AssessmentResult` the user has actually taken, same real-data-or-honest-placeholder pattern the Trained tab already established. New `getNearTransferAssessmentsSummary` (`apps/web/src/lib/near-transfer-assessment.ts`) looks up the latest real result per assessment (Backward Digit Span, Backward Spatial Span — a shared `NEAR_TRANSFER_ASSESSMENTS` config list avoids hardcoding the pair twice), parses each `AssessmentResult.scoreSummary`'s JSON shape defensively (skipping rather than crashing on anything malformed), and returns the real span, the real terminal-span correct/total counts, and the real confidence interval as display-ready percentages. `progress-data.ts`'s `ProgressData` gained a `nearTransferAssessments` array wired into `getProgressData`. **A real bug this card's own end-to-end test caught before being called done**: the whole Progress page (including the tab bar) was gated behind `hasAnyData`, which only counted trained-task/reading data — a user who took a near-transfer assessment but hadn't done any regular training would see "Nothing trained yet" and never reach their own real result. Fixed by including `nearTransferAssessments.length > 0` in `hasAnyData`, since near-transfer assessments are deliberately reachable without prior training, not gated behind a session the way Trial-based progress is. Each taken assessment now shows a real card (span, correct/total at that span, and the real Wilson-score confidence interval as a percentage range, explicitly captioned "a handful of trials carries real uncertainty" per the project's honesty rules), and the CTA row beneath dynamically reads "Take" for an assessment not yet attempted and "Retake" for one that has been — both real states, never both shown as available with no way to tell which is which. Spot-checked against the non-negotiable content rules: no IQ/general-intelligence/everyday-memory claim anywhere in the new copy, and the confidence interval is always shown alongside the span number, never a bare number implying false precision. **Verified in a real Chromium browser via Playwright** (a new test in `apps/web/e2e/progress.spec.ts`, alongside the file's existing session-based Progress test): completes onboarding, confirms the honest empty state and both real "Take" CTAs, drives a full Backward Digit Span run to the ceiling (reusing the digit-capture technique from `backward-digit-span.spec.ts`), then confirms the real card appears with the correct span (9) and confidence-interval text, and that the untaken Backward Spatial Span assessment still shows "Take" while Backward Digit Span now shows "Retake" — cross-checked against the real `AssessmentResult` row in Postgres directly. Full suite (50 tests, chromium project) and the full unit-test suite pass alongside this change; root `build`/`typecheck`/`lint` all pass too. Owner: Senior Full-Stack Engineer.
- **[Reading Training] Expand and rotate the reading-passage bank** — Phase 6's fifth card. `packages/reading-engine/src/passages.ts` grew from 5 same-tier passages to 9 spanning three genuinely different tiers (a new `difficultyTier` field: "beginner" ~90-110 words, the original 5 as "intermediate" ~140-165 words, "advanced" ~210-235 words) — real, load-bearing differences in sentence length/structure and vocabulary complexity, not just a relabeling, verified by a new test asserting beginner's average word count is genuinely shorter than intermediate's, which is genuinely shorter than advanced's. Also broadened genre coverage (added Everyday Science, History, Astronomy, and Economics topics) past the original 5's narrower Biology/History/Technology/Science mix. Each new passage keeps the same non-guessable, specific-detail comprehension question convention the original 5 established. **Real cross-session rotation, not just new content**: `PacedReadingTask.nextPassage()` (`packages/reading-engine`) is now order-driven instead of random — it always takes the front of a `passagePool` config (default: `READING_PASSAGES` in declared order), cycling without repeats until exhausted then resetting: a real behavioral simplification, not just a refactor, since the previous per-run random pick had no way to reflect real cross-session history anyway. This let the `random` config option be removed entirely as dead code once nothing in the class used it. New `apps/web/src/lib/reading-passage-rotation.ts`'s `getLeastRecentlySeenPassageOrder` computes the real per-user order from `Trial` history — a passage never read counts as more "least recently seen" than any read passage, and among read passages, the one read longest ago sorts first — exposed via a new `GET /api/reading-passage-order` route that `PacedReadingExercise.tsx` (a client component with no direct DB access) fetches once on mount, falling back to the engine's own declared order if the fetch fails rather than ever blocking the exercise on it. Trial metadata for reading gained a real `passageId` field (previously only `actualWpm`), the one piece of history the rotation needs and didn't have before. **A real regression caught and fixed before this was called done, not left implicit**: removing randomness from `nextPassage()` also affected `OnboardingFlow.tsx`'s calibration reading step, which called `new PacedReadingTask()` with no pool — every user's calibration would have shown the exact same first passage every time, a real loss of the previous (random) variety that had nothing to do with this card's actual goal (calibration is a one-time step per user, not subject to the repeat-fatigue problem the rotation targets). Fixed by giving calibration its own locally-shuffled pool (a new `shuffleArray` helper) rather than silently accepting the regression. Standalone `/train/<exercise>` routes never persist Trial rows (an existing, documented property of every exercise, not new here), so the rotation only has real history to work from once a user has gone through at least one orchestrated `/train/session` — an honest limitation, not a bug, and exactly why this card's own end-to-end verification drives two full sessions rather than two standalone runs. **Verified in a real Chromium browser via Playwright**: a new `apps/web/e2e/reading-passage-rotation.spec.ts` completes onboarding, drives two full training sessions end to end, and asserts the exact real rotation — every passage unseen in session 1 is read before any repeat in session 2, and the first repeat is precisely the passage read *first* (least recently) in session 1 — cross-checked against the real `Trial.metadata.passageId` history in Postgres directly, not just the in-browser sequence. Also re-ran `reading.spec.ts` and `onboarding.spec.ts` to confirm no regression from the expanded bank or the calibration fix. Full suite (51 tests, chromium project) and the full unit-test suite (129 tests across every package) both pass alongside this change; root `build`/`typecheck`/`lint` all pass too. Owner: Senior Full-Stack Engineer / Cognitive Scientist.

---

## Ready

**WIP limit: 1.** Exactly one card belongs here at a time — pull the next card from Backlog only when this section is empty (Definition of Ready lives in `docs/scrum.md`). When a card is pulled, move its full text here from Backlog rather than duplicating it.

_Currently empty — Expand and rotate the reading-passage bank (above, in Done) was Phase 6's fifth card. Only the longitudinal trend view remains in Phase 6's Backlog, and it genuinely isn't actionable yet (needs weeks of real elapsed usage first — see its own note below)._

---

## Backlog

Organized by phase, then blocked-vs-unblocked within a phase. Phases with nothing left aren't listed.

### Phase 5 — Engagement — complete

All five Phase 5 cards (Streaks, Achievements, XP + Training Level, Personal Bests, Weekly challenges) are done — see the Done section above.

`project_prompt.txt`'s LEAGUES / SOCIAL FEATURES section explicitly frames competitive/social features as "optional later-stage" — not part of Phase 5's Deliverables in `docs/development-plan.md` either, so it isn't a card here. Revisit only if a later phase explicitly calls for it.

### Phase 6 — Advanced Measurement (in progress — five of six cards done)

Broken down here for the first time, ahead of Phase 5 finishing, per `docs/development-plan.md`'s Phase 6: "Separate practiced-task improvement from genuine transfer with more rigor." Grounded directly in `project_prompt.txt`'s TRAINING VS ASSESSMENT / TRANSFER DASHBOARD / PERFORMANCE METRICS sections and `docs/evidence-review.md`'s own near-transfer findings (real near-transfer evidence exists specifically for backward-span/complex-span/serial-recall-structured working-memory tasks and Corsi-paradigm spatial tasks — this is what makes the near-transfer assessment cards scientifically grounded rather than invented). Far transfer is a different story: `docs/evidence-review.md` is unambiguous that no training module here has convincing far-transfer evidence, and `project_prompt.txt`'s own Transfer Dashboard guidance is "if broad transfer is unsupported, say so" — so no far-transfer *assessment* card exists here either; `ProgressView.tsx`'s "Broader Transfer" tab already does exactly this (a static, honest "not established" disclaimer, not a placeholder needing real data) and needs no change. Revisit only if new literature specifically supports a far-transfer measure.

Real infrastructure this phase can build on: `Assessment`/`AssessmentResult` (exists — Baseline Calibration (`BASELINE`), Backward Digit Span, and now Backward Spatial Span (both `NEAR_TRANSFER`) are real rows; the `AssessmentType` enum still has `FAR_TRANSFER`/`PERIODIC` unused). `packages/psychometrics`'s real `calculateDPrime`/`calculateSignalDetectionRates`/`calculateProportionConfidenceInterval` functions (see Done section above) are now genuinely wired into two real UIs, not just unit-tested. `apps/web/src/lib/near-transfer-assessment.ts`'s `getNearTransferAssessmentStatus` (the shared 14-day periodic-trigger rule) is now used by both assessments via its `assessmentName` parameter, exactly as planned. `ProgressView.tsx`'s "Similar tasks" tab now shows each real result the user has actually taken (or an honest per-assessment "Take" CTA for one they haven't) — see the "Wire" card in Done above.

- **[Progress] Longitudinal trend view** — the "deeper analytics, longitudinal reports" part of Phase 6's Deliverables in `docs/development-plan.md`. Lowest priority in this phase and last for a real reason, not arbitrarily: `docs/development-plan.md`'s own Phase 6 Dependencies line says this phase needs "weeks of trained-task data first," and this specific card needs the most of it (a trend is meaningless over a handful of sessions). Use the `dataviz` skill before building any chart, per `env_development.md`'s Phase 3+ guidance — a misleading trend line (e.g. WPM without its paired comprehension trend) is exactly the failure mode `docs/product-requirements.md` warns against. Dependencies: real usage over multiple weeks (a real elapsed-time dependency, not a code dependency). Complexity: M.

### Phase 4 — UX Polish (blocked, not just unscheduled)

Both items below need inputs that don't exist yet — do not promote to Ready by substituting a Backlog default for the real input.

- **[UX Polish] Onboarding refinement from real usage** — needs actual usage data from real users going through `/onboarding`; none exists yet (the app has no real users).
- **[UX Polish] Second usability-testing round** — needs real test participants; `docs/development-plan.md`'s Phase 1 already did the first round against the prototype, but this one needs the *built* app and real users.

### Phase 3+ and beyond (not yet Ready)

- **[Working Memory] Flanker/Go-No-Go cognitive-control module** — Blocked on stronger transfer evidence; see evidence review §6. Do not promote to Ready until evidence level improves or framing is re-scoped to strictly within-task.
- **[Reading Training] Skimming training module** — Needs its own research pass (tracked in `docs/evidence-review.md` "Next research pass").
- **[Reading Training] Reading flexibility training (pace-matches-purpose)** — Needs its own research pass.
- **[Payments] Stripe integration + entitlement system** — Correctly sequenced after Phase 3.
- **[Mobile Foundation] React Native/Expo vs Flutter spike** — Correctly sequenced after Web MVP validation (Phase 8).
- **[iOS] / [Android] app shells** — Blocked on Mobile Foundation.
- **[Analytics] Full product + scientific analytics pipeline** — Needs schema design once real usage exists to instrument.

### Phase 2 loose ends (not blocking Phase 5)

- **[Authentication] Verify the real Google/Facebook OAuth redirect** — needs real OAuth app credentials (`GOOGLE_CLIENT_ID`/`FACEBOOK_CLIENT_ID` etc. in `.env`) registered with each provider; everything up to that point is implemented and code-reviewed but the actual redirect/callback round-trip has never been exercised.
- **[Infrastructure] Broaden rate limiting beyond forgot-password/reset-password** — `docs/security.md` calls for rate limiting on login and register too; only the two newest endpoints have it (`apps/web/src/lib/rate-limit.ts`), and even that's in-memory/single-instance only — a real deployment needs a shared store (Redis, etc.).
- **[Authentication] Invalidate other sessions on password reset** — sessions use the JWT strategy, so resetting a password doesn't revoke sessions already issued on other devices. Needs database sessions or a token-revocation list.

---

## Card template

```text
Title:
Epic:
Priority: P0 | P1 | P2 | P3
Description:
Acceptance criteria:
- ...
Dependencies:
Complexity: XS | S | M | L | XL
Status: Backlog | Ready | Done
```

Priority definitions: **P0** essential (blocks the current phase's exit criteria) · **P1** high value · **P2** valuable · **P3** later. Every P0/P1 card must name real acceptance criteria and real dependencies — no "Build frontend"-style cards.
